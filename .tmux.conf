set -g mouse on

bind | split-window -h
bind - split-window -v
set-window-option -g window-status-current-style bg=color22

set -g @plugin 'tmux-plugins/tpm'
set -g @plugin 'tmux-plugins/tmux-sensible'

run '~/.tmux/plugins/tpm/tpm'
